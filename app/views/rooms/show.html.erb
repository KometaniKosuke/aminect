<h1><%= @room.name %></h1>

<div class="test">
  <table>
    <thead>
      <tr>
        <td>from...</td>
        <td>　</td>
        <td>text</td>
        <td>　</td>
        <td>送信時間</td>
      </tr>
    </thead>
    <tbody>
      <% @messages.each do |message| %>
        <tr>
          <td><%= message.user.name %></td>
          <td>　</td>
          <td><%= message.text %></td>
          <td>　</td>
          <td><%= message.date %></td>
        </tr>
      <% end %>
    </tbody>
</table>
</div>
<br>

<%= form_for @message do |f| %>
  <%= f.text_field :text %>
  <%= f.hidden_field :room_id %>
  <%= f.submit "送信" %>
<% end %>

<br><br><br><br>
<%= link_to "<<ルーム一覧へ戻る", :rooms %>
｜
<%= link_to "<<ユーザ一覧へ戻る", :users %><br>
<!-- <a href="#" onclick="history.back(-1);return false;">前のページ</a> -->
